server {
    listen 80;
    server_name 122.51.139.146;

    location / {
        root /usr/share/nginx/html;
        try_files $uri /index.html;
    }

    location /static/ {
        root /usr/share/nginx/html;
    }
}
